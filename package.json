{
  "name": "mini-cms-api",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "lint:spec": "spectral lint specs/cms-spec.yaml",
    "test": "jest",
    "validate": "npm run lint:spec && npm test"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@stoplight/spectral-cli": "^6.11.1",
    "jest": "^29.7.0",
    "jest-openapi": "^0.14.2",
    "js-yaml": "^4.1.0",
    "supertest": "^7.2.2"
  }
}

